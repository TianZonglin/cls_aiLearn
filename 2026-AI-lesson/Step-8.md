# Step 8 - 实现会话、多轮追问与历史记录

## 目标

在单轮问答基础上补齐会话、多轮追问、历史消息保存、会话重命名和清空能力，使系统具备稳定的连续问答体验。

## 输入

- `design6.13v2130.md`
- 已实现的单轮问答能力
- 目标接口：
  - `POST /chat/sessions`
  - `GET /chat/sessions`
  - `GET /chat/sessions/{id}`
  - `POST /chat/sessions/{id}/rename`
  - `POST /chat/sessions/{id}/clear`
  - `DELETE /chat/sessions/{id}`
  - `POST /qa/ask`

## 需要实现

1. 会话创建与读取：
   - 创建会话时保存 `selected_kb_ids_json`
   - 可列出会话
   - 可读取消息历史

2. 多轮追问策略：
   - 每一轮仍重新检索
   - 只拼接最近若干轮上下文
   - 不做跨会话长期记忆

3. 消息持久化：
   - 保存用户问题
   - 保存助手回答
   - 保存引用与检索快照

4. 会话管理：
   - 支持重命名
   - 支持清空
   - 支持删除

5. 更新最近使用：
   - 问答发生时更新相关知识库 `last_opened_at`
   - 更新会话 `last_message_at`

## 输出

- 会话 API
- 多轮追问逻辑
- 历史记录持久化

## 校验

1. 连续追问 3 到 5 轮时，主题不明显漂移。
2. 每轮问答仍受知识库范围限制。
3. 重开应用后还能看到历史会话和消息。
4. 会话可重命名、清空、删除。

## 不要做

- 不实现跨会话记忆
- 不做复杂推荐
- 不做联网补充答案

## 完成判定

当用户能稳定进行一段完整对话，并在重启后找回历史记录时，本步骤完成。
