# Step Qwen 6 - 前端展示模型状态与错误反馈

## 目标

在不重做前端主交互的前提下，让用户能看到当前模型配置和本地模型状态，并在问答失败时获得明确提示。

## 输入

- `design for Qwen2.5.md`
- Step-Qwen-1 的 `/system/config`
- Step-Qwen-5 的 `/system/llm-status`
- 当前设置页与问答前端

## 需要实现

1. 设置页展示模型配置：
   - `llm_provider`
   - `llm_model_name`
   - `llm_base_url`
   - `llm_enabled`

2. 设置页展示模型状态：
   - 可用 / 不可用
   - 错误信息或状态说明

3. 问答失败提示细化：
   - Ollama 未启动
   - 模型未下载
   - 模型调用超时
   - 正常后端失败

4. 保持当前前端主结构不变：
   - 左侧知识库分类
   - 右上知识库/文件区
   - 右下问答区

## 输出

- 设置页可展示当前 Qwen 模型配置与状态
- 问答失败时用户提示更明确

## 校验

1. 设置页能看到当前模型名和服务地址。
2. 模型服务不可用时，状态展示准确。
3. 问答报错时前端提示内容能帮助定位问题。

## 不要做

- 不增加前端模型切换器
- 不重做问答窗口 UI

## 完成判定

当用户可以明确知道当前是否已正确连接本地 Qwen2.5 服务时，本步骤完成。
