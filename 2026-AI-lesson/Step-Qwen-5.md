# Step Qwen 5 - 增加模型状态检测与异常回退

## 目标

为 Qwen2.5 接入补齐模型状态检测、可诊断错误提示和生成失败时的回退机制，避免本地模型异常导致问答完全不可用。

## 输入

- `design for Qwen2.5.md`
- 已实现的 Ollama Provider
- 已接入 Qwen 的 `/qa/ask`

## 需要实现

1. 新增模型状态检测接口：
   - 建议 `GET /system/llm-status`

2. 状态至少包括：
   - provider
   - model
   - reachable
   - available
   - message

3. 对问答主流程增加错误处理：
   - Ollama 未启动
   - 模型不存在
   - 超时
   - 空响应

4. 增加回退机制：
   - 当 `llm_fallback_to_extractive = true` 时
   - 模型调用失败可回退到当前抽取式答案生成

5. 错误语义清晰：
   - 能让前端区分“模型不可用”和“普通问答失败”

## 输出

- 模型状态检测接口
- 模型失败时的稳定回退逻辑

## 校验

1. `GET /system/llm-status` 可用。
2. 模型未启动时状态接口返回明确结果。
3. 模型调用失败时，问答接口不会崩。
4. 开启回退时，仍可返回抽取式回答。

## 不要做

- 不做前端设置页展示
- 不引入远程 API provider

## 完成判定

当模型状态可检测、异常可诊断、问答失败可回退时，本步骤完成。
