# Step 4 - 实现本地文件批量上传与存储

## 目标

实现本地文件批量上传、文件落盘、文档记录创建和状态流转起点，让用户可以把本地资料实际导入知识库。

## 输入

- `design6.13v2130.md`
- 已实现的知识库和文档表
- 支持类型：
  - `PDF`
  - `DOCX`
  - `PPTX`
  - `Excel`
  - `CSV`
  - `PNG`
  - `JPG`
  - `JPEG`

## 需要实现

1. 实现 `POST /documents/upload`
   - 使用 `multipart/form-data`
   - 支持 `knowledge_base_id`
   - 支持 `files[]`

2. 文件落盘规则：
   - 每个知识库存独立目录
   - 原文件名保留或安全改写
   - 避免重名覆盖

3. 创建文档数据库记录：
   - `source_type = file`
   - 正确识别 `file_type`
   - 写入 `storage_path`、`file_size`、`mime_type`
   - 初始化 `parse_status = pending`

4. 返回逐项上传结果：
   - 成功列表
   - 失败列表
   - 每项至少包含文件名、文档 ID 或失败原因

5. 做基础文件校验：
   - 文件类型不支持
   - 空文件
   - 不存在知识库

## 输出

- 可批量上传本地文件的 API
- 本地文件存储目录
- 与文件对应的文档记录
- 逐项上传结果返回结构

## 校验

1. 一次上传多个文件时，全部成功或部分失败都能看出逐项结果。
2. 上传后数据库中有对应 `documents` 记录。
3. 磁盘中存在对应原文件。
4. 不支持类型会被拒绝，并返回明确原因。

## 不要做

- 不做解析内容抽取
- 不做 OCR
- 不做网页链接导入
- 不做向量索引

## 完成判定

当本地文件能稳定批量上传、落盘、建档，并能通过 `GET /documents` 查到状态为 `pending` 的记录时，本步骤完成。
