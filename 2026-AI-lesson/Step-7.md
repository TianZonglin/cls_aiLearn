# Step 7 - 实现向量索引、检索边界与基础问答

## 目标

把解析后的 chunk 数据写入向量索引，完成单轮问答闭环：按知识库范围检索、调用模型生成答案、返回来源与证据。

## 输入

- `design6.13v2130.md`
- 已存在的 `document_chunks`
- 检索边界规则：
  - 只允许在已选知识库内检索
  - 未选知识库不得参与答案生成
- 目标接口：
  - `POST /qa/ask`

## 需要实现

1. 接入向量索引：
   - 使用 `Chroma`
   - 为每个 chunk 生成 embedding
   - 建立 `vector_id` 与 chunk 的映射

2. 建立检索服务：
   - 输入问题和知识库 ID 列表
   - 输出 top-k 相关 chunk
   - 保证检索边界严格受知识库筛选约束

3. 接入生成模型适配层：
   - 输入问题、检索结果、系统约束
   - 输出答案文本
   - 要求答案仅基于检索结果

4. 返回结构：
   - `answer`
   - `citations[]`
   - `matched_documents[]`
   - 每条 citation 至少有：
     - `knowledge_base_name`
     - `document_name`
     - `location_label`
     - `snippet`
     - `highlight_ranges`

5. 无命中策略：
   - 若证据不足，返回受限答案
   - 不允许自由编造

## 输出

- 向量索引写入逻辑
- 检索服务
- 单轮问答 API
- 来源结构化返回

## 校验

1. 同一问题在不同知识库范围下结果不同，且不会串库。
2. 问答结果始终带来源。
3. 无命中时返回“未找到足够依据”类反馈，而不是编造答案。
4. `matched_documents` 与 `citations` 的文档归属正确。

## 不要做

- 不实现多轮会话
- 不做导出
- 不做前端页面复杂交互

## 完成判定

当系统已经具备“上传文档 -> 解析切片 -> 检索 -> 生成带来源答案”的最小闭环时，本步骤完成。
