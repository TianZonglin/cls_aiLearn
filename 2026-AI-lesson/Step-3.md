# Step 3 - 实现知识库与文档元数据 CRUD

## 目标

实现知识库与文档列表层面的基础 CRUD 和查询接口，但此时文档仍只管理“元数据”和“状态”，不进入文件解析、切片和问答逻辑。

## 输入

- `design6.13v2130.md`
- 已存在的数据库模型与配置逻辑
- 目标接口：
  - `POST /knowledge-bases`
  - `GET /knowledge-bases`
  - `GET /knowledge-bases/recent`
  - `PATCH /knowledge-bases/{id}`
  - `DELETE /knowledge-bases/{id}`
  - `GET /documents`
  - `GET /documents/{id}`

## 需要实现

1. 实现知识库 CRUD：
   - 创建知识库
   - 列出知识库
   - 编辑名称和描述
   - 删除知识库

2. 实现最近使用字段逻辑：
   - 在知识库被访问或用于问答时更新 `last_opened_at`
   - 提供最近使用列表接口

3. 实现文档元数据查询：
   - 按知识库筛选
   - 按解析状态筛选
   - 文档详情返回状态、基础字段、预览文本占位、摘要占位

4. 删除知识库时的规则：
   - 允许物理删除关联文档记录
   - 此步骤先保证数据库层删除正确
   - 文件与向量删除留给后续完整文档删除流程接管

5. 补充基础错误处理：
   - ID 不存在
   - 名称为空
   - 删除不存在对象

## 输出

- 完整的知识库 CRUD API
- 文档列表与文档详情 API
- 最近使用知识库 API

## 校验

1. 可创建多个知识库，并能正确列出。
2. 知识库可重命名、可删除。
3. `GET /knowledge-bases/recent` 能按最近使用时间返回。
4. `GET /documents` 在无上传逻辑前也能稳定返回空列表。
5. 异常输入返回明确错误，不崩溃。

## 不要做

- 不上传文件
- 不写网页抓取
- 不做解析
- 不做问答

## 完成判定

知识库和文档元数据层面的 API 已稳定可调用，可作为前端基础数据源，即可进入下一步。
