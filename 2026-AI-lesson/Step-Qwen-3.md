# Step Qwen 3 - 实现知识库问答 Prompt 构造层

## 目标

为知识库问答构造稳定的 Qwen Prompt，严格约束模型“只基于证据回答”，避免变成普通聊天式输出。

## 输入

- `design for Qwen2.5.md`
- 当前问答返回结构：
  - `citations`
  - `matched_documents`
- 当前问题输入与重排后的命中结果

## 需要实现

1. 新增 Prompt 构造层：
   - 例如 `llm_prompting.py`

2. 构造 system prompt：
   - 只依据提供证据回答
   - 不得编造
   - 证据不足时明确说不足
   - 用中文回答
   - 不把文件名本身当作事实
   - 不输出额外来源列表

3. 构造 user prompt：
   - 用户问题
   - 当前知识库范围
   - 证据块列表
   - 输出要求

4. 证据块格式统一：
   - 知识库名
   - 文档名
   - 定位标签
   - 文本片段

5. 控制 Prompt 长度：
   - 只选高质量 top hits
   - 避免重复片段
   - 必要时做截断

## 输出

- 可复用的知识库问答 Prompt 构造层

## 校验

1. Prompt 中包含足够的检索证据。
2. Prompt 不重复堆叠大量相似片段。
3. Prompt 明确限制模型自由发挥。
4. Prompt 在证据不足时可引导模型返回保守答案。

## 不要做

- 不修改模型调用层
- 不修改前端
- 不引入新接口

## 完成判定

当系统已经能稳定产出适合知识库问答的 Qwen Prompt 时，本步骤完成。
