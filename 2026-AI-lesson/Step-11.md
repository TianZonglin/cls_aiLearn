# Step 11 - 实现摘要、重新索引、DOCX 导出与最终收尾

## 目标

完成第一版剩余的增强项与收尾工作，包括文档摘要、知识库重建索引、DOCX 导出、最近使用优化、异常收口和最终验收。

## 输入

- `design6.13v2130.md`
- 前 10 步的产出
- 目标增强项：
  - 文档摘要卡片
  - `POST /knowledge-bases/{id}/reindex`
  - `POST /export/docx`
  - 最近使用知识库
  - 会话增强

## 需要实现

1. 文档摘要：
   - 在解析完成后生成短摘要
   - 写入 `summary_text`
   - 在前端文档详情或证据面板展示

2. 知识库重新索引：
   - 对知识库下所有有效文档重建 chunk 和向量索引
   - 避免重复残留数据

3. DOCX 导出：
   - 结构与 Markdown 保持一致
   - 至少包含问题、回答、来源、时间、知识库信息

4. 最近使用优化：
   - 工作台展示最近知识库
   - 会话列表排序合理

5. 最终异常收口：
   - 无命中引导
   - 删除前影响提示
   - 导出失败提示
   - 链接导入失败提示

## 输出

- 文档摘要能力
- 重新索引能力
- DOCX 导出能力
- 第一版最终体验收口

## 校验

1. 解析完成后的文档可看到简短摘要。
2. 知识库重建索引后，不会生成重复 chunk。
3. DOCX 能成功导出，结构与 Markdown 一致。
4. 最近使用知识库和会话排序符合用户直觉。
5. 按 `design6.13v2130.md` 中的必测场景做回归时，核心链路全部通过。

## 不要做

- 不扩展到音视频
- 不补登录与同步
- 不引入新一轮大功能

## 完成判定

当第一版已经具备设计文档定义的核心能力，并通过主要验收场景，可以进入演示与试用阶段时，本步骤完成。
