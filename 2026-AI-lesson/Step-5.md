# Step 5 - 实现网页链接导入与批量网页链接导入

## 目标

实现公开网页的单条导入和批量导入，把网页正文转成内部文档记录，纳入与本地文件一致的后续解析与索引流程。

## 输入

- `design6.13v2130.md`
- 已实现的 `documents` 表和文档状态体系
- 目标接口：
  - `POST /documents/import-url`
  - `POST /documents/import-urls`

## 需要实现

1. 实现单条网页链接导入：
   - 接收 `knowledge_base_id` 和 `url`
   - 验证 URL 基本合法性
   - 拉取网页内容
   - 抽取标题、正文、基础元信息

2. 实现批量网页链接导入：
   - 接收 `urls[]`
   - 逐条处理并返回逐项结果
   - 失败项保留错误原因

3. 建立网页文档记录：
   - `source_type = url`
   - `file_type = html`
   - 填写 `source_url`
   - 将清洗后的正文保存到内部存储或作为预处理文本保存
   - 初始化 `parse_status = pending` 或直接视流程进入 `processing`

4. 规则约束：
   - 只支持公开网页
   - 不支持登录态网页
   - 不追求保留原网页样式

## 输出

- 单条网页导入 API
- 批量网页导入 API
- 公开网页内容对应的文档记录

## 校验

1. 导入单条公开网页后，可在文档列表看到一条 `source_type = url` 的记录。
2. 批量导入多条链接时，成功和失败都会逐条返回。
3. `GET /documents/{id}` 能看到网页来源的 `source_url`。
4. 非法 URL 或无法访问的网页能返回明确错误。

## 不要做

- 不做复杂单页应用完整抓取
- 不做登录态抓取
- 不做问答

## 完成判定

网页链接已经能被当作“文档来源”纳入系统，且与文件上传流程在数据层保持一致，即可进入下一步。
