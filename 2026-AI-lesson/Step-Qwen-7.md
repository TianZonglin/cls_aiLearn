# Step Qwen 7 - 联调 7B 默认模式并验证 14B 可切换

## 目标

完成 Qwen2.5 的最终联调，确保 `7B-Instruct` 作为默认模式可稳定运行，并验证 `14B-Instruct` 可通过配置切换。

## 输入

- `design for Qwen2.5.md`
- 前 6 步结果
- 本地 Ollama 环境

## 需要实现

1. 用 `qwen2.5:7b-instruct` 做默认联调：
   - 单知识库问答
   - 多轮追问
   - 文件混合知识库问答

2. 验证问答质量：
   - 中文回答自然
   - 不明显偏离知识库
   - 证据不足时能保守输出

3. 通过配置切换到 `qwen2.5:14b-instruct`：
   - 无需改代码
   - 能正常调用

4. 校验现有能力不被破坏：
   - 会话记录
   - 来源展示
   - 回答弹窗
   - 检索结果点击

5. 更新 README：
   - 写明 Ollama 安装
   - 写明 pull 命令
   - 写明 7B / 14B 切换方式

## 输出

- 默认可运行的 Qwen2.5-7B 接入结果
- 可切换的 14B 模式
- 更新后的使用说明

## 校验

1. `7B-Instruct` 可在本项目中完成稳定知识库问答。
2. `14B-Instruct` 可通过配置切换成功。
3. 回归现有主要问答链路，前端和后端都不报错。
4. README 可指导新用户完成模型准备和本地运行。

## 不要做

- 不引入第二个 LLM provider
- 不替换现有检索框架
- 不扩展到 embedding 模型替换

## 完成判定

当默认 7B 已经能稳定跑通，14B 已具备切换能力，并完成文档更新时，本步骤完成。
