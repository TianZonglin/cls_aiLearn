# Step 10 - 实现证据高亮、打开原文件、状态中心与 Markdown 导出

## 目标

补齐第一版最关键的体验增强：证据面板、命中高亮、打开原始文件、解析状态中心和 Markdown 导出，让产品从“能用”提升到“完整可演示”。

## 输入

- `design6.13v2130.md`
- 已实现的问答和前端主界面
- 目标接口与能力：
  - `POST /documents/open`
  - `POST /export/markdown`
  - 文档解析状态展示
  - 高亮片段预览

## 需要实现

1. 证据面板：
   - 列出命中来源
   - 展示知识库名、文档名、位置标签
   - 支持片段展开与折叠

2. 高亮展示：
   - 在 `preview_text` 中高亮命中范围
   - 不做原始 PDF/Word 精准高亮

3. 打开原始文件：
   - 后端调用系统默认程序打开本地文件
   - 网页来源不提供该操作

4. 文件解析状态中心：
   - 列出 `pending / processing / done / failed`
   - 显示失败原因
   - 支持重试入口

5. Markdown 导出：
   - 导出结构必须包含：标题、问题、回答、来源列表、时间、所选知识库
   - 支持从前端触发并拿到结果文件

## 输出

- 证据高亮面板
- 原始文件打开能力
- 文档状态中心
- Markdown 导出功能

## 校验

1. 用户点击来源后，右侧能看到对应高亮片段。
2. 点击“打开原始文件”可用系统默认程序打开本地文件。
3. 解析失败文档能看到失败原因并可触发重试。
4. 导出的 Markdown 内容与界面回答结构一致。

## 不要做

- 不实现 DOCX 导出
- 不做原始文件内精准定位高亮
- 不做复杂阅读器

## 完成判定

当用户已经可以围绕“答案 - 证据 - 原文件 - 导出”完成完整核对流程时，本步骤完成。
